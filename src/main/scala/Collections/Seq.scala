package Collections

/*
  Seq is just a trait representing a sequence of primitive data types
 */

trait Seq[A] {

  xs.isDefinedAt(i)     // Tests whether i is contained in xs.indices
  xs.lengthCompare(n)   // Returns -1 if xs is shorter than n + 1, if longer then returns 0

  // Index Search


}